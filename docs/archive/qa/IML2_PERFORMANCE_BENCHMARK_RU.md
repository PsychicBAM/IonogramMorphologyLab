# IML-2 Бенчмарк производительности (RU)

**Дата:** 2026-08-01  
**Скрипт:** `scripts/benchmark_real_frame_access.py`  
**Данные:** синтетический fallback `demo_mixed_diffuse.mat` (0.774 МБ).  
**Слепые данные Article 3:** не использовались.

Повтор с `--mat <одобренный_неслепой_Am_all.mat>` для суток KFU. Не заявлять ускорение без измеренных чисел.

## Измерения

См. таблицу в английской версии / `docs/IML2_BENCHMARK_RAW.json`.  
Формат кэша: `iml2-zarr-frame-v1`. Первая сборка кэша ≈ 0.52 с на синтетике; LRU-попадание кадра ≈ 0 с.

## Ограничения

Без полного суточного MAT нельзя корректно заявить времена для 144/1440 кадров. Отзывчивость GUI обеспечивается фоновым потоком кэша.
